
    const n = [5, 3]
    const a = Number(n[0]), b = Number(n[1]);
    let row = '*'.repeat(a)   
    // a 길이 만큼 반복함
    for (let i = 0; i < b; i++) {
        console.log(row)
    // b 길이 만큼 반복문 돌리고 출력
    }
